- name: practice ansible
  hosts: all
  become: yes
  # vars:
  #   dog: raju
  # vars_files:
  #  - sample_vars.yaml


  tasks:
    - name: check status of nginx
      ansible.builtin.systemd:
       name: nginx
      register: nginx_status


    # - name: print status
    #   ansible.builtin.debug:
    #    var: ansible_facts


       
    # - name: print status
    #   ansible.builtin.debug:
    #    var: " this is distro name {{ ansible_facts.devices.distribution }}"
    #   ignore_errors: yes


    - name: print checking vars
      ansible.builtin.debug:
       msg : " checking vars is {{ service_name }}"


    # - name: print checking vars
    #   ansible.builtin.debug:
    #    var: dog